function disableInput() {
    let elem = document.getElementById('elem');
    elem.disabled = true;
}

function enableInput() {
    let elem = document.getElementById('elem');
    elem.disabled = false;
}